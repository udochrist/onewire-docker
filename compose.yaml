services:
  onewire:

    build:
      context: .
      dockerfile: ./Dockerfile

    image: onewiredocker:latest
    container_name: onewire
    restart: unless-stopped
        
    volumes:
      - ./config:/config

    cap_add:
      - SYS_ADMIN
      
    environment:
      START_OWHTTPD: "${START_OWHTTPD:-false}"
      START_OWFS: "${START_OWFS:-false}"
    
    devices:
      - /dev/fuse:/dev/fuse:rwm

    ports:
      - 2120:2120 # FTP
      - 2121:2121 # HTTP
      - 4304:4304 # SERVER
